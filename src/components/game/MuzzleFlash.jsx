import { motion, AnimatePresence } from 'framer-motion';
import { useState, useEffect } from 'react';

export default function MuzzleFlash({ show }) {
  // Removed muzzle flash effect completely
  return null;
}